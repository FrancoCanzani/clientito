# Dummy Page

This is a placeholder document for the Petit docs section.

It exists to verify the docs routing, sidebar navigation, typography, and page layout without relying on production content.

## Purpose

- confirm that markdown files appear automatically in the docs sidebar
- check the serif styling and old-manual presentation
- exercise the document template with headings, lists, and links

## Example Notes

Petit keeps documentation as plain markdown files in the repository.

That means a new page can be added with very little ceremony: write a file, commit it, and the docs section can render it.

### Sample Link

You can return to the [docs index](/docs) at any time.

### Sample Code

```ts
const status = "draft";
console.log(`Docs page status: ${status}`);
```

## Closing

If this page looks right, we can replace it with real product and engineering documentation whenever you want.
